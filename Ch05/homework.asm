CODES SEGMENT
    ASSUME CS:CODES
START:
    MOV AX, 1234H
    MOV DS, AX

    MOV BX, 0005H
    MOV [BX], 0
    MOV CX, 100
S:  ADD [BX], CX
    LOOP S

    MOV AH,4CH
    INT 21H
CODES ENDS
END START
